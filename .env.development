# Leave empty during local dev — the app uses your browser URL automatically.
# For phone testing on the same Wi-Fi, set your machine's LAN IP (not localhost):
# VITE_SITE_URL=http://192.168.1.100:5173
